---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = "", style: styleSheet = "" } = Astro.props;
---

<div class={`card p-4 ${className}`} style={styleSheet}>
  <slot />
</div>
