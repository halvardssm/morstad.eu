---
interface Props {
  class?: string;
  style?: string;
}
const { class: className = "", style = "" } = Astro.props;
---

<h2 class={`font-medium m-0 text-heading text-l ${className}`} style={style}>
  <slot />
</h2>
